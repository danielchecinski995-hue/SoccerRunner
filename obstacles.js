/**
 * Obstacle Manager - Placeholder (obstacles now handled in game.js)
 * K11 Studio - Soccer Runner
 */

// Empty placeholder for backwards compatibility
class ObstacleManager {
    constructor() {}
    reset() {}
    setSpeed() {}
    setDifficulty() {}
    update() {}
    checkCollision() { return { collision: false }; }
    checkBonusCollection() { return []; }
}

window.ObstacleManager = ObstacleManager;
